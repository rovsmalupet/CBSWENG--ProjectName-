# Deployment guides — consolidated

This was an index pointing at five separate guides, two of which
(`backend/DEPLOYMENT.md`, `frontend/DEPLOYMENT.md`) never existed. All of them
described a **Railway** deployment; the backend now runs on **Render**.

There is now one guide:

### 👉 [DEPLOYMENT.md](DEPLOYMENT.md)

Kept as a pointer so existing links do not lead anywhere misleading. Safe to
delete once everyone has updated their links.
